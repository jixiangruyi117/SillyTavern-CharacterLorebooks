# 开发记录

## 2026-08-12：0.2.2 同数量角色的延迟绑定修复

- 反证：仅比较角色数量不能证明角色绑定已完整；酒馆可在同一批角色对象中延后写入 `data.extensions.world`，此时数量不变但归属已经变化。
- 修复：目录保存不含正文的归属指纹；`CHARACTER_PAGE_LOADED` 到达时，指纹变化即防抖重建。500 角色、数量不变、绑定后到达的回归场景验证会从 500 本公共书变为 500 本角色归属书。
- 兼容：附加世界书和全局世界书设置从酒馆 `world-info.js` 的运行时 `world_info` 读取；此前经 `powerUserSettings` 读取并不是酒馆实际公开的世界书设置位置。

## 2026-08-12：0.2.1 角色归属缓存同步修复

- 现象：角色较多时，已随角色导入并绑定的世界书可能在插件中被列入“公共世界书”；手动刷新后只有部分条目恢复归属。
- 根因：0.2.0 的目录缓存可在酒馆完成角色列表加载或“保存世界书 → 写回角色绑定”之间建立，之后没有监听 `CHARACTER_PAGE_LOADED` 来校正这份不完整目录。
- 修复：实际开始重建时重新取得最新酒馆上下文；监听角色列表完成渲染，并仅在角色数量与缓存不一致时防抖重建，避免翻页造成重复全表扫描。
- 安全：仅更新插件内存目录，不读取世界书正文，不写入角色卡、世界书文件、全局启用或提示词设置。

## 2026-08-12：0.2.0 大角色库性能优化

- 证据：旧索引在每个角色上对完整 `charLore` 数组执行查找，角色数和附加绑定同时增长时会形成平方扫描；5000 个角色的本地基准中位数约 275ms。
- 修复：首次/结构变更时把附加绑定汇总为按角色键读取的 Map，再以 250 个角色为批次让出浏览器；缓存完成后切角色只标记当前归属，不再扫描角色与绑定列表。
- 界面：搜索输入等待 150ms 后更新结果，全部范围最多渲染 40 个条目并分页；结构事件合并 160ms，避免连续导入时重复重建。
- 验证：新增异步分批建索引与缓存投影一致性的回归测试。

## 2026-08-12：角色世界书 0.1.0

- 需求：将角色绑定世界书与公共世界书区分，玩当前角色时不被其他角色世界书干扰。
- 实现：从 SillyTavern 原生角色主世界书 `data.extensions.world`、附加世界书 `world_info.charLore` 与世界书列表动态建立只读索引；界面只调用原生编辑入口。
- 安全边界：未修改世界书文件、角色卡、全局启用状态、聊天元数据或提示词装配；无网络、凭证和删除操作。
- 验证：`npm run check` 通过，包含 3 项索引回归测试。

## 2026-08-12：0.1.1 交互与移动端修复

- 现象：搜索或切换筛选范围时，抽屉被整块重绘，酒馆原生抽屉回到折叠状态。
- 根因：插件将查询文本保存在设置后调用完整 `render()`，替换了抽屉节点；这不是酒馆世界书数据或其他插件问题。
- 修复：筛选只替换列表区域，完整刷新时显式恢复抽屉开闭状态；移动端改为单列字段与两列操作按钮，触控按钮保持至少 44px。
- 酒馆服务器：本机 8000 端口监听正常；外部命令行请求被本机登录保护返回 401，未取得“模型/API 服务器连不上”的根因证据，未改酒馆连接配置。

## 2026-08-12：0.1.2 当前角色识别修复

- 现象：插件标题显示“群聊或未选择角色”。
- 根因：索引层只接受 number 型角色索引；酒馆运行时若提供 string 型的 `characterId`，就会被错误丢弃。
- 修复：安全接受纯数字字符串索引；群聊和未选择角色使用不同的可解释提示。

## 2026-08-12：0.1.3 窄面板排版修复

- 截图证据：约 617px 宽的酒馆面板未命中原 600px 断点；Flex 压缩使“原生世界书”按钮逐字换行。
- 修复：工具栏默认使用四列自适应网格，760px 以下切换为筛选/搜索整行、操作按钮双列；操作按钮不允许压缩为竖排。

## 2026-08-12：0.1.4 角色范围全局启用清理

- 需求：仅保留当前角色与公共世界书的启用效果，关闭其他角色世界书。
- 语义：角色绑定世界书由酒馆自动参与上下文；操作只从原生全局多选中移除“仅属于其他角色”的已启用世界书，保留当前角色与公共书的既有启用状态。
- 安全：执行前列出具体名称并二次确认；通过 `#world_info` 原生 change 事件保存，不直接写世界书文件、角色卡或聊天数据。
